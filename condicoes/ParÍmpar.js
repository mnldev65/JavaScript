// Par ou Ímpar
// Objetivo
// Verificar se um número é:

// - Par
// - Ímpar

const num = 809;
if (num%2==0) {
    console.log(`O número ${num} é par`);
} else {
    console.log(`O número ${nume} é ímpar`);
}